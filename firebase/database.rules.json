{
  "rules": {
    ".read": "auth != null",
    ".write": false,

    "commands": {
      "kill_switch": {
        ".read": "auth != null",
        ".write": "auth != null && newData.isBoolean()"
      }
    }
  }
}
